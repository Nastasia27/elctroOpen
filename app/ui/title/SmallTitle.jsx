export default function SmallTitle({name}) {
    return(
        <div className="flex justify-center items-center w-full">
            <h1 className=" pb-5 uppercase text-sm font-black">{name}</h1>
        </div>
    )
}